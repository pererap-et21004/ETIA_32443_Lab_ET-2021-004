const int initialCapacity = 10;
int vehiclesEntered = 0;
bool lastSensorState = false;

void setup() {
 
  DDRB |= (1 << PB0) | (1 << PB1) | (1 << PB2);
  
 
  DDRD &= ~((1 << PD2) | (1 << PD3));
  
  
  PORTD |= (1 << PD3);
}

void loop() {
 
  if (!(PIND & (1 << PD3))) {
    vehiclesEntered = 0;
  }

 
  bool currentSensorState = (PIND & (1 << PD2));
  if (currentSensorState && !lastSensorState) {
    if (vehiclesEntered < initialCapacity) {
      vehiclesEntered++;
    }
  }
  lastSensorState = currentSensorState;

  
  int availableSlots = initialCapacity - vehiclesEntered;

  
  PORTB &= ~((1 << PB0) | (1 << PB1) | (1 << PB2));

  if (availableSlots > (initialCapacity / 2)) {
    PORTB |= (1 << PB0); 
  } else if (availableSlots > 0) {
    PORTB |= (1 << PB1); 
  } else {
    PORTB |= (1 << PB2); 
  }
  
  delay(50); 
